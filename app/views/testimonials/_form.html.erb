<%= simple_form_for(@testimonial) do |f| %>
<% if testimonial.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(testimonial.errors.count, "error") %> prohibited this testimonial from being saved:</h2>
    <ul>
    <% testimonial.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="row">
  <div class="col-md-6">
    <div class="row">
      <div class="col-sm-9">
        <%= f.input :author %>
      </div>
      <div class="col-sm-3">
        <%= f.input :placement %>
      </div>
    </div>
    <%= f.input :quote, as: :ckeditor, required: true %>

  </div>
</div>
<div class="row">
  <div class="col-sm-6">
    <%= f.button :submit, "Save",  class: 'btn btn-primary', style: "margin-bottom: 100px;" %>
  </div>
</div>


<% end %>
